# Things To Do

1. Build the NN "expert" modules
2. Train the "expert" models on `./data`
2. Build the vector-store module (use existing libraries- FAISS?)
3. Build the full model
   a. Encode
   b. Attend
   c. Experts
   d. Combine
   e. Decode
4. Train the overall model (not the experts)
   * 